<header class="magic-header">
    <div class="header-content">
        <div class="app-title">
            <span style="font-size: 1.5rem; font-weight: 700; background: linear-gradient(135deg, #fff, #e0e7ff); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                HealthApp
            </span>
        </div>
        <div class="user-info">
            {{> avatar userName=nomeUsuario profilePicture=profilePicture initials=(getInitials nomeUsuario)}}
        </div>
    </div>
</header>
